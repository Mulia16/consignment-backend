<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.consignment.service.persistence.mapper.CsrvMapper">

    <resultMap id="CsrvHeaderResultMap" type="com.consignment.service.persistence.model.CsrvHeaderEntity">
        <id property="id" column="id"/>
        <result property="docNo" column="doc_no"/>
        <result property="company" column="company"/>
        <result property="receivingStore" column="receiving_store"/>
        <result property="supplierCode" column="supplier_code"/>
        <result property="supplierContract" column="supplier_contract"/>
        <result property="branch" column="branch"/>
        <result property="supplierDoNo" column="supplier_do_no"/>
        <result property="deliveryDate" column="delivery_date"/>
        <result property="remark" column="remark"/>
        <result property="status" column="status"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdMethod" column="created_method"/>
        <result property="referenceNo" column="reference_no"/>
        <result property="releasedAt" column="released_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <resultMap id="CsrvDetailResultMap" type="com.consignment.service.persistence.model.CsrvDetailEntity">
        <id property="id" column="id"/>
        <result property="csrvId" column="csrv_id"/>
        <result property="itemCode" column="item_code"/>
        <result property="availableQty" column="available_qty"/>
        <result property="requestQty" column="request_qty"/>
        <result property="receivingQty" column="receiving_qty"/>
    </resultMap>

    <insert id="insertHeader" parameterType="com.consignment.service.persistence.model.CsrvHeaderEntity">
        INSERT INTO csrv_header (
            id, doc_no, company, receiving_store, supplier_code, supplier_contract, branch, supplier_do_no,
            delivery_date, remark, status, created_by, created_method, reference_no, released_at, created_at, updated_at
        ) VALUES (
            #{id}, #{docNo}, #{company}, #{receivingStore}, #{supplierCode}, #{supplierContract}, #{branch}, #{supplierDoNo},
            #{deliveryDate}, #{remark}, #{status}, #{createdBy}, #{createdMethod}, #{referenceNo}, #{releasedAt}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
        )
    </insert>

    <insert id="insertDetail" parameterType="com.consignment.service.persistence.model.CsrvDetailEntity">
        INSERT INTO csrv_detail (id, csrv_id, item_code, available_qty, request_qty, receiving_qty)
        VALUES (#{id}, #{csrvId}, #{itemCode}, #{availableQty}, #{requestQty}, #{receivingQty})
    </insert>

    <select id="findHeaderById" resultMap="CsrvHeaderResultMap">
        SELECT id, doc_no, company, receiving_store, supplier_code, supplier_contract, branch, supplier_do_no,
               delivery_date, remark, status, created_by, created_method, reference_no, released_at, created_at, updated_at
        FROM csrv_header
        WHERE id = #{id}
    </select>

    <sql id="csrvFilterWhere">
        <where>
            <if test="c.docNo != null and c.docNo != ''">AND h.doc_no ILIKE CONCAT('%', #{c.docNo}, '%')</if>
            <if test="c.company != null and c.company != ''">AND h.company = #{c.company}</if>
            <if test="c.receivingStore != null and c.receivingStore != ''">AND h.receiving_store = #{c.receivingStore}</if>
            <if test="c.supplierCode != null and c.supplierCode != ''">AND h.supplier_code = #{c.supplierCode}</if>
            <if test="c.supplierContract != null and c.supplierContract != ''">AND h.supplier_contract = #{c.supplierContract}</if>
            <if test="c.branch != null and c.branch != ''">AND h.branch = #{c.branch}</if>
            <if test="c.supplierDoNo != null and c.supplierDoNo != ''">AND h.supplier_do_no = #{c.supplierDoNo}</if>
            <if test="c.createdMethod != null and c.createdMethod != ''">AND h.created_method = #{c.createdMethod}</if>
            <if test="c.referenceNo != null and c.referenceNo != ''">AND h.reference_no = #{c.referenceNo}</if>
            <if test="c.status != null and c.status != ''">AND h.status = #{c.status}</if>
            <if test="c.createdBy != null and c.createdBy != ''">AND h.created_by = #{c.createdBy}</if>
            <if test="c.itemCode != null and c.itemCode != ''">AND d.item_code = #{c.itemCode}</if>
        </where>
    </sql>

    <select id="searchHeaders" parameterType="com.consignment.service.model.csrv.CsrvSearchCriteria" resultMap="CsrvHeaderResultMap">
        SELECT DISTINCT h.id, h.doc_no, h.company, h.receiving_store, h.supplier_code, h.supplier_contract,
               h.branch, h.supplier_do_no, h.delivery_date, h.remark, h.status, h.created_by, h.created_method,
               h.reference_no, h.released_at, h.created_at, h.updated_at
        FROM csrv_header h
        LEFT JOIN csrv_detail d ON d.csrv_id = h.id
        <include refid="csrvFilterWhere"/>
        ORDER BY h.created_at DESC
        LIMIT #{c.perPage} OFFSET #{c.offset}
    </select>

    <select id="countHeaders" parameterType="com.consignment.service.model.csrv.CsrvSearchCriteria" resultType="long">
        SELECT COUNT(DISTINCT h.id) FROM csrv_header h
        LEFT JOIN csrv_detail d ON d.csrv_id = h.id
        <include refid="csrvFilterWhere"/>
    </select>

    <select id="findDetailsByHeaderId" resultMap="CsrvDetailResultMap">
        SELECT id, csrv_id, item_code, available_qty, request_qty, receiving_qty
        FROM csrv_detail
        WHERE csrv_id = #{csrvId}
        ORDER BY item_code ASC
    </select>

    <update id="updateHeaderStatus">
        UPDATE csrv_header
        SET status = #{status},
            released_at = #{releasedAt},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <select id="countMatchingSetup" resultType="long">
        SELECT COUNT(*)
        FROM consignment_external_supplier external_supplier
        WHERE external_supplier.item_code = #{itemCode}
          AND external_supplier.supplier_code = #{supplierCode}
          AND external_supplier.supplier_contract = #{supplierContract}
          AND external_supplier.consignee_store = #{receivingStore}
    </select>

    <select id="findMaxDocNoNumber" resultType="long">
        SELECT COALESCE(MAX(CAST(SUBSTRING(doc_no FROM 6) AS BIGINT)), 0)
        FROM csrv_header
        WHERE doc_no ~ '^CSRV-[0-9]+$'
    </select>

    <update id="updateHeader" parameterType="com.consignment.service.persistence.model.CsrvHeaderEntity">
        UPDATE csrv_header SET
            branch          = #{branch},
            supplier_do_no  = #{supplierDoNo},
            delivery_date   = #{deliveryDate},
            remark          = #{remark},
            reference_no    = #{referenceNo},
            updated_at      = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="deleteDetails">
        DELETE FROM csrv_detail WHERE csrv_id = #{csrvId}
    </delete>
</mapper>
