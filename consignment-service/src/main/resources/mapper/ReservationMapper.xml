<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.consignment.service.persistence.mapper.ReservationMapper">

    <insert id="insertReservation">
        INSERT INTO consignment_reservation (
            doc_no, doc_type, store, sku, qty, reservation_type, created_at, updated_at
        ) VALUES (
            #{docNo}, #{docType}, #{store}, #{sku}, #{qty}, #{reservationType}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
        )
    </insert>

    <delete id="deleteByDocNo">
        DELETE FROM consignment_reservation
        WHERE doc_no = #{docNo}
    </delete>
</mapper>
